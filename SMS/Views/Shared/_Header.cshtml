<nav class="navbar navbar-expand-lg">
    <div class="container">
        <a class="navbar-brand" href="@Url.Action("Index", "Home", new { area = "" })">Student Management System</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav"
                aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("Index", "Student", new { area = "" })">Student</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("Index", "Teacher", new { area = "" })">Teacher</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("Index", "Subject", new { area = "" })">Subject</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("Index", "Allocation", new { area = "" })">Allocation</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

